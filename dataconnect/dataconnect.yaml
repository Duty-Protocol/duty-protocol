specVersion: "v1beta"
serviceId: "duty-protocol"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "duty-protocol-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
